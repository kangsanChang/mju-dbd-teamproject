extends layout

block content

  div(class="ui middle aligned center aligned grid")
    div(class="six wide column" style="margin-top:100px;")
      h2(class="ui teal header")
        div(class="content") Company Management System
      form(class='ui large form', action="/api/login", method="POST")
        div(class="ui stacked segment")
          div(class="field")
           label ID
           input(type="text" name="id" placeholder="ID")
          div(class="field")
            label Empoly number
            input(type="password" name="emp_num" placeholder="EMP_NUM")
          button(class="ui fluid large teal submit button") Login
